// Gastmodus-Limit (Briefing Abschnitt 1/6): max. 5 erkannte Artikel ohne Account.
export const MAX_GUEST_ITEMS = 5;

// Header, über den der Client (Mobile-App) eine lokal erzeugte, persistente
// anonyme Geräte-ID mitschickt. Kein Personenbezug – nur ein Zähler.
export const GUEST_DEVICE_ID_HEADER = "x-guest-device-id";
